<% if obj.errors.any? %>
<div class="row">
    <div class="col-md-12">
        <div class="alert alert-danger col-md-8 col-md-offset-3" >
            <h5>لطفا اررورها را رفع کنید</h5>
           <ul>
        <% obj.errors.full_messages.each do |msg| %>
            <li><%= msg%></li>
        <%end %>
            </ul>
        </div>
    </div>
    
</div>
<%end %>